#pragma once

#define BASE_SIZE(multipler) float multipler * 32;

namespace Constants {
// Screen settings
inline constexpr int SCREEN_WIDTH = 1280;
inline constexpr int SCREEN_HEIGHT = 720;
inline constexpr int GAME_WIDTH = 640;
inline constexpr int GAME_HEIGHT = 360;
} // namespace Constants
